{
  "name": "@openpond/desktop",
  "version": "0.0.37",
  "openpondReleaseVersioned": true,
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "dev": "pnpm --dir ../.. run bundle:server && tsc -b && electron .",
    "build": "tsc -b"
  },
  "dependencies": {
    "@openpond/logging": "workspace:*",
    "@openpond/runtime": "workspace:*",
    "electron": "43.1.1"
  }
}
