{
  "name": "@openpond/desktop",
  "version": "0.0.19",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "dev": "bun run --cwd ../.. bundle:server && tsc -b && electron .",
    "build": "tsc -b"
  },
  "dependencies": {
    "@openpond/runtime": "workspace:*",
    "electron": "^39.2.7"
  }
}
