# Starter import fixture

This test-only snapshot was generated from Sandbox's `lib/sandbox/model-starters/packages/invoice-extraction/build.mts`. Its starter content hash is `3610f1396475dd20bed85fa962fb450c70bdfb458a67bd906c94886170349fb1`.

It protects client import, transactional persistence, split preservation, and retry behavior. It is not a bundled starter catalog or an authoring source. Runtime discovery and resolution use the hosted Sandbox API through the public SDK catalog client. Maintain starter content and verifier qualification in Sandbox; refresh this snapshot only when needed to exercise a changed consumer contract.
